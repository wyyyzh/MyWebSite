# 统一换行符，避免本地 Windows(CRLF) 与 GitHub Actions(Linux/LF) 之间来回打架
* text=auto eol=lf

# 二进制资源不做任何转换，转了会损坏文件
*.png binary
*.ico binary

# 明确按文本处理的格式
*.svg text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.sh text eol=lf
